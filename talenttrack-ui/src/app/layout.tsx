import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TalentTrack | Multi-Tenant Applicant Tracking System",
  description: "Enterprise applicant tracking. Built for companies to manage recruiting workflows, built for candidates to track hiring pipelines.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body className="mesh-gradient min-h-screen antialiased text-foreground">
        {children}
      </body>
    </html>
  );
}
